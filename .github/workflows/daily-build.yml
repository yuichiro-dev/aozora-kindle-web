name: Daily Build Index & Dependencies

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  run-build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm ci

      - name: Update dependencies
        run: npm update

      - name: Run build-index.js
        run: npm run build-index

      - name: Commit and Push changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          # public/authors.json を追加（または必要に応じて git add -A に変更）
          git add public/books.json public/authors.json package.json package-lock.json 
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update books.json, authors.json, and dependencies via GitHub Actions" && git push)